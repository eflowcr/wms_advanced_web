# Make the Node pin bite. Without engine-strict the `engines` field in
# package.json is only a warning npm prints and then ignores; with it, an
# install on the wrong Node major fails immediately with the expected vs.
# actual version instead of failing strangely several commands later.
engine-strict=true
