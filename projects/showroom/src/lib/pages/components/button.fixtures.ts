/**
 * Registro de ejemplo que simula lo que mandaría el backend: el artículo y la ubicación de la
 * fila del bloque 4b van tal cual, sin traducir.
 */
export const ROW_SAMPLE = { sku: 'SKU-04871-B', location: 'A-04-17' } as const;
